# 빌드 산출물
WideAgent.exe
*.pdb

# 런타임 상태 (실행 중에 생성된다)
WideAgent.log
WideAgent.state
WideAgent.chatgpt.state
WideAgent.chatgpt.port
WideAgent.chatgpt.exe.txt
WideAgent.claude.applied

# 사용자 설정
WideAgent.width.txt

# 아이콘 시안 작업 폴더 (배포 저장소에 넣지 않는다)
assets/

# 아이콘 확인용 미리보기 (make-icon.ps1 -Preview)
icon-preview-*.png
